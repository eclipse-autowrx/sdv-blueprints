# digital.auto matlab simulink connector

# Use Case

# Architecture

# Getting started

## Prerequisites
1. 

## Build

### Trial build for host machine
1. install gRPC development packages

`sudo apt install -y libgrpc++-dev libprotobuf-dev protobuf-compiler protobuf-compiler-grpc pkg-config`

## Demo
1. 
